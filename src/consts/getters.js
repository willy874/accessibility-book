const bookmark = {
  BOOKMARK_LIST: 'bookmarkList',
}

const chapter = {
  CHAPTER_LIST: 'chapterList',
}

const root = {
  IS_LOGIN: 'isLogin',
}

export const Getters = {
  ...root,
  ...chapter,
  ...bookmark,
}
